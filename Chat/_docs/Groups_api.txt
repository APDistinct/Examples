Некоторые соображения по поводу групп.

Вопрос удаления группы - можно ли потом её опять воскресить (нет) или надо создавать заново точно такую же, с теми же функциями и составом (да)? Тогда надо иметь дату удаления.
Ещё вопрос - войти в группу может любой желающий, найдя её в списках и подав команду на вступление или админ группы формирует списки вступающих, а сами желающие не могут вступить в неё (второе. И нельзя увидеть группу не являясь её участником. Пока так. Может конечно народ захочет иметь публичные группы. Но народ пока не знает чего хочет, и это нормально)?
В группах с равным приоритетом нельзя удалять членов группы кроме себя


15. Группы
    1. создание группы со списком участников
    2. добавление в группу (списком)
    3. удаление из группы (списком)
    4. выход из группы
    5. получение групп, в которых состоит
    6. получение участников группы (для тех, кто состоит)
    7. Смена статуса участника группы (админство)
    8. (НВ) группа распадается, когда в ней остаётся один участник. --  Нет, когда он подаст заявление на выход, т.е. останется 0. Либо когда удаляется последний админ, если он не назначил себе преемника.
    9. Расформирование группы (или это = удалению из группы всех?) -- не обязательно. Может поступить запрос о ликвидации без списка, а это придётся делать нам, удаляя всех, кто в ней есть и рассылая уведомления об этом. 
    10. (НВ) События о добавлении в/удалении из группы


1. создание группы с/без списка участников (автор группы становится участником автоматом)
POST group
{
	"name": "", //наименование создаваемой группы
	"is_equal": "0/1", // признак равенства/неравенства пользователей в группе
    "members": [ "id1", "id2", ...], //список идентификаторов учестников, может быть пустым или null
    "admins": [...] //список идентификаторов админов из members (если is_equal=0)
}
response: 200 OK
{
    "group_id": ..., //идентификатор созданной группы
	"name": "", //наименование создаваемой группы
	"is_equal": "0/1", // признак равенства/неравенства пользователей в группе
    "members": [ "id1", "id2", ...],
    "admins": [...], //(если is_equal=0)
    "created_date" : ""
}

Какие могут быть ошибки? - потом

2. Получение информаци о группе
GET group/"group_id"

response: 200 OK
Ответ как в команде POST group

3. Удаление группы
DELETE group/"group_id"

response: 200 OK

Или коды ошибок
Нет прав на удаление группы
группа уже удалена
нет такой группы

4. добавление/удаление в/из группу (списком) с возможностью корректировки названия группы
Команда либо выполняется полностью, либо не выполняется вся. Если есть часть, не соответствующая логике работы с группами, выдаётся ошибка.
  - вопрос, а добавляем только в одну группу или же несколько и для каждой разные списки? От этого зависит передача идентификатора группы. Снят. Только в одну.
POST group/<group_id>

{
    "name": "", //наименование создаваемой группы
    "members": [ "id1", "id2", ...],
    "admins": [...], //(если is_equal=0)
    "remove_members": []
    "remove_admins": []
}
response: 200 OK

Ошибки?

6.  выход из группы
GET group/"group_id"/out

response: 200 OK
Или коды ошибок


7. получение групп, в которых состоит
GET group
{
  groups: [
    каждый элемент как в п.1, но без участников
  ]
}

response: 200 OK
Или коды ошибок
нет такой группы

8. получение полной информации о группе (для тех, кто состоит) - Чем отличается от п.2?
GET group/group_id

response: 200 OK
как в п.1
